# Copy to .env (or just `source` this file after editing) before running.
# Never commit a real .env — these are credentials.

export VCENTER_HOST=vcenter.example.com
export VCENTER_USER='svc-cleanup@vsphere.local'
export VCENTER_PASS='replace-me'
